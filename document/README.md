# Detailed documents kept for compatibility

The active documentation index is [`../docs/README.md`](../docs/README.md).
Files in this directory are retained because existing experiment notes and
links use the `document/` paths. New project documentation should be added to
`docs/` instead.
